<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Custom Color Form</title>
    <style>
        /* Custom color palette */
        :root {
            --email-color: #e3f2fd;
            /* Light blue */
            --telephone-color: #fce4ec;
            /* Light pink */
            --date-color: #ede7f6;
            /* Light purple */
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            padding: 40px;
        }

        form {
            background-color: #fff;
            padding: 20px 30px;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: auto;
        }

        label {
            display: block;
            margin-top: 15px;
            font-weight: bold;
        }

        input[type="email"] {
            background-color: var(--email-color);
        }

        input[type="tel"] {
            background-color: var(--telephone-color);
        }

        input[type="date"] {
            background-color: var(--date-color);
            opacity: 0.7;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1em;
        }
    </style>
</head>

<body>
    <form> <label for="email">Email:</label> <input type="email" id="email" name="email"
            placeholder="you@example.com" />
        <label for="tel">Telephone:</label>
        <input type="tel" id="tel" name="tel" placeholder="123-456-7890" />

        <label for="date">Date:</label>
        <input type="date" id="date" name="date" />
    </form>
</body>

</html>
